function [Beta F G]=convergeFG(B,M,S,T)

F=rand(T,nf);
iter=100;
parameters=zeros(p+(nf*p*T)+(T*nf),iter);
for i=1:iter
    [Beta G]=concentrateG(B, M, F, S, p, T, nf);
    [Beta F]=concentrateF(B, M, G, S, p, T, nf);
    parameters(:,i)=[Beta; G(:); F(:);];
end
D=diff(parameters);
sum(abs(D),